<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>today date</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>

    <div class="box" id="heading">
        <!-- <h1>hello every one...</h1> -->
        <h1><%= day %></h1>
    </div>
    
    <!-- <% if(day == "sunday" || day == "suterday"){ %>
    <h1 style="color: green;"><b>today's day is <%= day %> </b></h1>
    <% }else{ %>
        <h1 style="color: red;">today's day is <%= day %> </h1>
    <% } %> -->
    
    <div class="box">
        <form action="/delete" method="post">
        <% task.forEach(function(element) { %>
            <div class="item">
                <input type="checkbox" onChange="this.form.submit()" name="checkbox" value="<%= element._id %>">
                <p><%= element.name %></p> 
            </div>
            <% }) %>
            <input type="hidden" name="listName" value="<%= day %>"></input>
         
        </form>
         <form class="item" action="/" method="post">
            <input type="text" name="task" placeholder="enter your task" autocomplete="off">
            <button type="submit" name="button" value="<%= day %>">+</button>
        </form>
    </div>

    
</body>
</html>